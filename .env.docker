DB_USER=postgres
DB_PORT=5432
DB_PASSWORD=password
DB_NAME=notes_db

NODE_ENV=production
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# Configuracion de CORS OPCIONAL (en formato CSV)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
